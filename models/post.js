class Post {
    constructor(id, userId, author,title ) {
            this.id = id;
            this.userId = userId;
            this.author = author;
            this.title = title;
    }
}

module.exports = Post;